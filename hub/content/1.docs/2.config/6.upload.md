---
title: 上传配置
description: 配置文件的存储方式，支持本地存储、对象存储和托管存储。
navigation:
  icon: i-lucide-cloud-upload
---

## 存储类型

文件的存储方式，默认为本地存储。

| 存储类型 | 说明 | 备注 |
| --- | --- | --- |
| 本地存储 | 文件存储在服务器本地 `uploads` 目录 | - |
| 对象存储 | 兼容 S3 协议的对象存储服务 | 支持 AWS S3、阿里云 OSS、腾讯云 COS、七牛云 Kodo、Cloudflare R2、MinIO 等 |
| 托管存储 | 通过 Panel 代理外部托管存储服务 | - |

## 最大文件大小

限制单次上传文件的最大大小，仅影响博客端的上传限制，管理端不受影响。

- 用户在**博客端**发表评论添加的配图：**受**最大文件大小限制
- 管理员在**管理端**添加文章配图：**不受**最大文件大小限制

## 文件命名

自定义文件存储时的命名规则，默认为 `{timestamp}_{random}{ext}`。

| 占位符 | 说明 | 示例 |
| --- | --- | --- |
| `{timestamp}` | 时间戳 | 20260319123045 |
| `{random}` | 8位随机字符串 | a1b2c3d4 |
| `{YYYY}` | 4位年份 | 2026 |
| `{MM}` | 2位月份 | 03 |
| `{DD}` | 2位日期 | 19 |
| `{HH}` | 2位小时（24小时制） | 14 |
| `{mm}` | 2位分钟 | 30 |
| `{ss}` | 2位秒数 | 45 |
| `{type}` | 上传用途类型 | avatar、article、attachment |
| `{userid}` | 上传用户 ID | 1 |
| `{filename}` | 原始文件名（不含扩展名） | image |
| `{ext}` | 文件扩展名 | .jpg |

## 对象存储配置

使用兼容 S3 协议的云存储服务，需要配置以下参数：

| 参数 | 必需 | 说明 |
| --- | --- | --- |
| Access Key | ✅ | 云存储服务的访问密钥 |
| Secret Key | ✅ | 云存储服务的密钥 |
| 地域 | ✅ | 存储服务的区域，如 `us-east-1`、`ap-guangzhou` |
| 存储桶 | ✅ | 云存储中存放文件的容器名称 |
| 服务端点 | ✅ | 存储服务地址，如 `s3.us-east-1.amazonaws.com` |
| 自定义域名 | 可选 | 用于生成文件访问 URL，填写后会替代默认域名 |
| 启用 HTTPS | 可选 | 根据服务器是否配置 HTTPS 决定 |

---

## 关于上传

### 支持的文件类型

| 类型  | 文件格式                                            |
| --- | ----------------------------------------------- |
| 图片  | jpg, jpeg, png, gif, webp, svg, bmp, tiff       |
| 视频  | mp4, webm, quicktime, avi, mkv, mpeg, 3gpp, flv |
| 音频  | mp3, wav, ogg, aac, flac                        |
| 文档  | txt, pdf, doc, docx                             |
| 压缩包 | zip, rar, 7z                                    |
| 其他  | json（配置文件）                                      |

**安全限制**：不允许上传可执行文件，包括 .exe, .bat, .cmd, .com, .pif, .scr, .vbs, .js, .jar, .sh 等格式。
